# 給 AI 助手的說明

**動手之前先讀 [`docs/專案現況.md`](docs/專案現況.md)**，那裡有完整的專案脈絡、
排程演算法的設計原因、以及踩過的坑。這份只列最低限度的規則。

## 規矩

1. **介面文字一律用繁體中文。** 註解也用繁中。
2. **改完 `server/src/routes/schedule.js` 一定要跑 `cd server && npm test`**（26 項要全過）。
3. **不要提交任何 `.sqlite` 檔**，裡面有帳號和密碼雜湊。資料在 Turso 雲端，不在 repo 裡。
4. **日期一律用 `server/src/util/date.js`**。不要混用「本機時區解析 `new Date(ds + 'T00:00:00')`
   ＋ UTC 輸出 `toISOString()`」——在 UTC+8 會差一天。
5. push 到預設分支會**自動部署到 Render**，所以主線要隨時是可上線的狀態。

## 排程演算法的硬規則（改壞了測試會紅）

1. 任何項目都不能排到自己截止日 `end` 之後，最後補位也不行
2. 模考（`final: true`）獨佔該科一整天
3. 純題目（標題結尾「單元練習」「歷屆試題」）不與「範例+例題」同日
4. 純題目一天最多 2 份（`ONE_CAP = 2`），永遠不會有第 3 份
5. 任務不能遺失：排不下要進 `unplaced` 回報，不能默默吃掉

## 常用指令

```bash
cd server && npm test          # 後端演算法回歸測試（26 項）
cd server && npm start         # 起後端
cd client && npm run dev       # 起前端
```

## 目前的優先順序

使用者的指示是「**先不要新增任何新功能**」，優先做穩定性、測試、重構。
要加功能請先問過。
